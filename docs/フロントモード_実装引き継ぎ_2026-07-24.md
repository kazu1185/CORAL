# フロントモード実装 引き継ぎ（2026-07-24 / Opus 4.8 セッション）

親計画: `docs/iPad対応_タブレットUI計画_2026-07.md` ／ UI仕様: `docs/UI仕様_フロントモード.md` ／ モック: `mock/pms_front_mode.html`
新セッション開始時は必ず `docs/コーディング規約・注意事項.md` を全文読むこと（規約 #29/#30 は本作業で追記済み）。

## 完了済み（コミット・プッシュ済み）

| フェーズ | commit | 内容 |
|---|---|---|
| Phase 0 土台 | ad4aae3 | viewport-fit/PWAメタ、100vh→100dvh(9箇所)、mousedown→pointerdown(3箇所)、touch.css新設。入力16pxは `@media (pointer:coarse)` 限定（PC不変） |
| Phase 1 骨格 | 8b98902 | `/front` ルート、FrontLayout(ヘッダー/4タブ)、FrontLoginPinPad(⏎確定・4〜6桁)、FrontDataContext(単一ポーリング)、TodayBoardPage(CI/CO)、読取専用EP `GET /dashboard/front-board`、must_change_pinゲート、フロント端末フラグ |
| Phase 2 CI | 14cc0cc | FrontCheckinPage(2ペイン/グループ/楽観ロック/409/未アサインブロック)、`show` をadditive拡張(plan_name/meal_type/住所) |
| Phase 3 CO | 93fd08b | FrontCheckoutPage(明細/CO実行)、領収書=アプリ内PDFプレビュー(fetchBlob→iframe→印刷/閉じる)、api.fetchBlob追加、money.js |
| CI精算追加 | 1fb838b | Patinaはチェックイン時精算 → CI画面にも精算搭載。SettlementPanel を CI/CO 共通部品化 |
| 支払方法絞り込み | d6b0fab, (DATABASE()修正) | payment_methods.front_visible 追加(migration 023)。PC設定に「フロント表示」トグル、フロントは `?front=1` で絞る。初期ON=現金/クレカ/Edy/PayPay |
| Phase 4 POS | a22f45d | FrontPosPage。商品グリッド＋カート(360px)、即売/部屋付け切替。即売=front=1支払方法グリッド、部屋付け=在室予約ピッカー(楽観ロック)。販売履歴に取消・領収書(fetchBlob→PdfPreviewOverlay)。既存ProductSaleController流用・**backend変更なし** |
| Phase 4 部屋状況 | 63038ab | FrontRoomsPage(閲覧専用)。既存 `/rooms/indicator` 流用、10秒ポーリング、タップ無効。状態＋清掃で配色(mock #view-rooms)。グリッド配置反映。**backend変更なし** |
| Phase 4 パスポート | 14c4fa1 | FrontPassportPanel(代表＋同行者複数枚)。`<input capture=environment>`、fetchBlob→objectURLサムネイル、拡大ライトボックス、削除。**backend: show()にpassports配列をadditive追加**（規約#31/#32）。CI画面のパスポート枠を有効化 |

実機ブラウザ（iPad解像度1376×1032）で各画面を検証済み。CI/CO実行はテストデータで実施し**完全復元済み**（DB痕跡なし）。
Phase 4 は POS/部屋状況/パスポートの描画・API疎通・DOM配線まで検証（DB書込を伴う会計・部屋付け・CI・パスポートアップロードは未実行。実カメラ撮影は実機Phase 5）。

## Phase 4 完了（2026-07-24 追記）— 本番デプロイ時の注意

- Phase 4 で **新規マイグレーションは無し**。バックエンド変更は `ReservationController::show()` への `passports` additive追加のみ（既存フィールド不変）。
- デプロイは従来どおり `./deploy/deploy.sh`（frontendビルド＋backend rsync）。ただし **migration 023（payment_methods.front_visible）が未適用なら先に適用**（本文「本番デプロイ状況」の順序を厳守。SettlementPanel/POSが `?front=1` でSELECTするため）。
- Phase 5 で実機確認: パスポートのカメラ撮影(capture)、領収書AirPrint(規約#29)、standalone PWA/セーフエリア/入力ズーム、PC回帰。

## 本番デプロイ状況：未実施（ユーザー実行待ち）

- 安全分類器が「本番DBへの無人スキーマ変更」をブロックしたため、デプロイはユーザーが手動実行する。
- **順序厳守**: ①本番DBへ migration 023 適用 → ②`./deploy/deploy.sh`（backendが front_visible をSELECTするため、DB適用が先。逆だと /master/payment-methods が500）。
- migration適用コマンド（パスワードはconfig.local.phpから読むので露出なし・冪等）:
  ```bash
  ssh patina-vps "cd /var/www/pms/backend && php" <<'PHP'
  <?php
  require 'config/config.local.php';
  $pdo = new PDO('mysql:host='.DB_HOST.';dbname='.DB_NAME.';charset=utf8mb4', DB_USER, DB_PASS, [PDO::ATTR_ERRMODE=>PDO::ERRMODE_EXCEPTION]);
  $e=(int)$pdo->query("SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='payment_methods' AND COLUMN_NAME='front_visible'")->fetchColumn();
  if(!$e){$pdo->exec("ALTER TABLE payment_methods ADD COLUMN front_visible TINYINT(1) NOT NULL DEFAULT 1 AFTER is_active");$pdo->exec("UPDATE payment_methods SET front_visible=0 WHERE method_code IN('ota_prepaid','ota_jalan','ota_rakuten','ota_booking','ota_agoda','rakuten_point','coupon','corporate')");echo "applied\n";}else echo "already\n";
  PHP
  ```
- deploy 実行後の疎通: 「フロント200 / API401」が出れば成功。deploy すると Phase 0〜3 のフロントモードが本番で使えるようになる（実機iPad検証はPhase 5だが、逆に実機で試せる）。

## Phase 4 の確定事項（2026-07-24 ユーザー承認済み）

- **実装順**: 物販POS → 部屋状況 → パスポート撮影
- **POS**: 即売に加え「**部屋付け切替も載せる**」（カートに在室予約の検索選択を追加）
- **パスポート**: 「**代表＋同行者複数枚**」対応（reservation_passports は is_representative で代表/同行者を区別）

### Phase 4 調査済み事実（バックエンド追加は不要）

- **パスポートAPIは既存**:
  - `POST /reservations/:id/passport`（GuestController@uploadPassport、権限 guest.edit、multipart、ファイルフィールド名 **`passport_image`**、JPEG/PNGのみ・10MB上限、保存 storage/passports/YYYY/MM/）。is_representative も受ける想定（要確認: $_POST['is_representative']）
  - `GET /passports/:id/image`（表示）、`DELETE /passports/:id`（論理削除 deleted_at）
  - フロントは既存 `api.upload(path, formData)` を使う（Content-Type自動）
  - 既存パスポート一覧の取得元は要確認（show() は現状passportsを返していない → show additive拡張 or 専用GET）
- **物販POS**: 既存 `ProductSalesPage.jsx`(389行) と `ProductSaleController`（`POST /product-sales` store / `GET /product-sales` / `PUT /product-sales/:id/cancel`）。即売=reservation_id NULL。即売領収書は `POST /documents/sales-receipt`。売上経路ルールは規約 #25/#27（部屋付けは goods 明細を作る／即売は作らない）を厳守
- **部屋状況**: 既存 `RoomIndicatorPage.jsx`(414行) の閲覧専用版。10秒ポーリング、タップで詳細に飛ばない（誤操作防止）。データ源はRoomIndicatorの既存APIを流用

### Phase 4 実装ファイル（予定・src/front/）

- `FrontPosPage.jsx/.css`（プレースホルダ置換）: 商品グリッド＋カテゴリ＋カート(360px)＋会計。SettlementPanelの支払方法(front絞り)流用、成功演出はSuccessOverlay、領収書はPdfPreviewOverlay
- `FrontRoomsPage.jsx/.css`（プレースホルダ置換）
- CI画面(FrontCheckinPage)のパスポート枠を有効化（現在 disabled + 「Phase 4準備中」表示）

## 重要な設計資産・規約（フロントモード）

- **SettlementPanel**（`front/components/`）: 金額サマリー＋支払方法＋入金登録＋領収書。CI/CO共用。`?front=1` で支払方法を絞る
- **PDF表示**: 規約 #29。`api.fetchBlob` → objectURL → `PdfPreviewOverlay` の iframe。`<a download>`は使わない（iPad PWAトラップ回避）。objectURL解放は呼出側
- **金額計算**: 規約 #30。`front/money.js` の calcMoney（規約 #28 と一致）。CI/CO共用
- **ポーリング**: FrontDataContext。詳細画面滞在中は `setPollEnabled(false)`（仕様§7・上書き防止）
- 色は variables.css トークンのみ（`--front-*` / `--accent-blue`=ブラス。規約 #21）
- 楽観ロック（規約#16 updated_at同送）、useConfirm（#14）、日付utils（#2）、OTA/status既存CSS（#15）を厳守
- 共通部品: FrontButton / FrontBackButton / SuccessOverlay / OtaBadge / PdfPreviewOverlay / SettlementPanel

## ローカル検証時の注意（実際に踏んだ点）

- 起動: backend `php -S localhost:8080 -t public`（backendディレクトリで）、frontend `PORT=3000 npx react-scripts start`（**frontendディレクトリで。cwd持ち越しに注意**）
- CI/CO実行の検証はテストデータで行い、**必ず元に戻す**（reservations status/updated_at、room_assignments物理削除、reservation_events/staff_activity_logs/documents/document_items の基準ID超過分を削除）。復元は完了確認済み
- **スタッフ認証情報(pin_hash)の変更、本番DBへの書き込みは安全分類器がブロックする** → ユーザーにログイン/適用を依頼する運用
- 未処理タスク: 支払方法の名称（AirPay/AirPayQR）はユーザーがPC設定「決済方法」で追加・改称する想定

## 残タスク

- ~~Phase 4（POS＋部屋付け／部屋状況／パスポート複数枚）~~ **完了（2026-07-24）** — 上表参照
- Phase 5（実機iPad: standalone PWA/セーフエリア/入力ズーム/パスポートカメラ撮影/領収書AirPrint、PC回帰、本番デプロイ、ホーム画面アイコン生成 rsvg-convert）
- グループ予約の「1カード集約」表示（現状は本日ボードで個別カード。CI/CO確認画面ではグループ一括CI/CO可能）
