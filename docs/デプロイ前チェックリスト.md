# デプロイ前チェックリスト

本番デプロイ前に必ず確認・実行すること。

---

## 1. テストデータの削除

### storage/tl_import/ 内のテスト電文を削除
```bash
rm -rf storage/tl_import/*
```
- 開発中にテスト取込で生成されたアーカイブXMLが入っている
- 本番では TLリンカーンから受信した電文のみが保存される

### DBのテストデータクリア
```sql
SET FOREIGN_KEY_CHECKS = 0;
TRUNCATE TABLE reservation_charges;
TRUNCATE TABLE reservation_events;
TRUNCATE TABLE reservation_sources;
TRUNCATE TABLE room_assignments;
TRUNCATE TABLE guest_links;
TRUNCATE TABLE guest_aliases;
TRUNCATE TABLE guest_merge_logs;
TRUNCATE TABLE reservation_passports;
TRUNCATE TABLE tl_import_logs;
TRUNCATE TABLE reservations;
TRUNCATE TABLE guests;
SET FOREIGN_KEY_CHECKS = 1;
```
- マスタデータ（rooms, room_types, staff, plans, 税率等）は残す

---

## 2. マイグレーション実行

本番DBで未実行のマイグレーションを順番に実行する。
```
backend/migrations/010_reservation_merge.sql
backend/migrations/011_fix_reservation_sources_uk.sql
backend/migrations/012_add_discount_charge_type.sql
backend/migrations/013_add_telegram_data.sql
```

---

_デプロイ準備が進むにつれて項目を追記すること。_
